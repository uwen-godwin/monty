#include "monty.h"

/**
 * comments - Ignores lines that start with '#'
 * @stack: Pointer to the top of the stack
 * @line_number: Line number in the Monty bytecode file
 */
void comments(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
